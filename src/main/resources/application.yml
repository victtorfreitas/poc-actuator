spring:
  data:
    mongodb:
      username: root
      password: example
      host: localhost
      authentication-database: admin
      database: payment
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    loggers:
      enabled: true